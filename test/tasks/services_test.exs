defmodule Mix.Tasks.Kraken.ServicesTest do
  use ExUnit.Case

  #  @folder Path.expand("../../definitions/services", __ENV__.file)
  #  @service_json File.read("#{@folder}/simple-math.json")

  describe "define" do
    test "with json definition" do
      #      IO.inspect(@folder)
      #      IO.inspect(@service_json)
    end
  end
end
